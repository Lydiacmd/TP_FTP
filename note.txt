Q1 / 
 - Definir un type enum du type de requete envoye 
 - on cree le fichier commun.h pour cella 
 - Dans l'etapes 1 on ignore GET on mets juste PUT et LS 

Q3 / 
 - Prend le nom du serveur en argv[1]
 - Open_clientfd(serveur, PORT) pour se connecter
 - Prompt ftp> avec boucle fgets + sscanf pour parser get nom_fichier

Q4 / 
 - pids[] global
 - handler_sigint : envoie SIGINT à tous les fils + waitpid
 - Signal(SIGINT, handler_sigint) installé avant le fork
 - Dans le fils : Signal(SIGINT, SIG_DFL) pour terminer proprement -> eviter les boucle

Q5 / 
 - Serveur cherchee ces fichiers dans ./serveur_files
 - Le clients sauvgarde les fihcier recus dans .:client_files

Q6 /
- SERVEUR :  fonction transfert  
        - Recevoir la request_t du client
        - Vérifier le type de requête
            Si GET : ouvrir le fichier, l'envoyer
            Sinon erreur 
        
Q7 / Traitement côté client
    - Construit la request_t avec req.type = GET et le nom du fichier
    - L'envoie au serveur avec Rio_writen
    - Reçoit la response_t du serveur
    - Si rep.code == -1 → affiche un message d'erreur
    - Si succès → reçoit le fichier avec Rio_readn, le sauvegarde dans CLIENT_DIR
    - Affiche les stats : bytes reçus, temps, vitesse en Kbytes/s


Q8 / transfert par Bloc 
    - On definit un BLOCK_SIZE de 4096 dans commun.h
    - envoie / reception par bloc entre les 2 cotes 
    - le serveur envois d'abord rep.filesize dans responce_t pour que le client sache la taille exacte quil va recevoire

Q9 /  Multi-requêtes par connexion : 
    - Le serveur a une boucle while(1) dans transfert(), il reste connecté tant que le client n'envoie pas bye
    - Le client boucle sur fgets et break sur bye

Q10 /  Reprise après crash client 
    - Le client écrit dans un fichier temporaire (TRANSFER_TMP) l'avancement octet par octet à chaque bloc reçu
    - Au redémarrage, il lit ce fichier et met req.offset à la valeur déjà reçue
    - Le serveur fait un lseek(fd, req.offset, SEEK_SET) pour sauter la partie déjà envoyée
    - À la fin du transfert, TRANSFER_TMP est supprimé